global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'test-services'
    static_configs:
      - targets: ['localhost:8000']
        labels:
          service: 'api-gateway'
      - targets: ['localhost:8001']
        labels:
          service: 'ai-gateway'

