# Authentication_V1 Configuration
version: "1.0.0"
status: experimental

# Token Settings
tokens:
  secret_key: "change-in-production"
  access_token_ttl_seconds: 3600
  refresh_token_ttl_seconds: 604800

# Session Settings
sessions:
  ttl_seconds: 3600
  max_per_user: 5

# Password Settings
password:
  min_length: 8
  hash_iterations: 100000
