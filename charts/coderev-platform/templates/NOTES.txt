🚀 CodeRev Platform has been deployed!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📍 DEPLOYMENT SUMMARY

  Three-Version Architecture:
  {{- if .Values.versions.v1.enabled }}
    ✅ V1 Experiment:  {{ .Values.versions.v1.namespace }}
  {{- end }}
  {{- if .Values.versions.v2.enabled }}
    ✅ V2 Production:  {{ .Values.versions.v2.namespace }}
  {{- end }}
  {{- if .Values.versions.v3.enabled }}
    ✅ V3 Legacy:      {{ .Values.versions.v3.namespace }}
  {{- end }}

  Control Plane:
  {{- if .Values.lifecycleController.enabled }}
    ✅ Lifecycle Controller
  {{- end }}
  {{- if .Values.evaluationPipeline.enabled }}
    ✅ Evaluation Pipeline
  {{- end }}
  {{- if .Values.opa.enabled }}
    ✅ OPA Policy Engine
  {{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🌐 ACCESS URLS

{{- if .Values.frontend.ingress.enabled }}
  Frontend:
    https://{{ (index .Values.frontend.ingress.hosts 0).host }}
{{- end }}

{{- if .Values.vcai.ingress.enabled }}
  API:
    https://{{ (index .Values.vcai.ingress.hosts 0).host }}
{{- end }}

  Admin (Internal):
    http://admin.coderev.internal

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 VERIFICATION COMMANDS

  # Check all pods
  kubectl get pods -n {{ .Values.versions.v2.namespace }}
  kubectl get pods -n platform-control-plane

  # Check services
  kubectl get svc -n {{ .Values.versions.v2.namespace }}

  # View Argo Rollout status (if enabled)
  kubectl argo rollouts get rollout vcai-rollout -n {{ .Values.versions.v2.namespace }}

  # Check HPA status
  kubectl get hpa -n {{ .Values.versions.v2.namespace }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 MONITORING

{{- if .Values.monitoring.prometheus.enabled }}
  Prometheus metrics are being collected.
  
  View SLO metrics:
    kubectl port-forward -n platform-monitoring svc/prometheus 9090:9090
    Open: http://localhost:9090
{{- end }}

{{- if .Values.monitoring.grafana.enabled }}
  Grafana dashboards are configured.
  
  Access Grafana:
    kubectl port-forward -n platform-monitoring svc/grafana 3001:80
    Open: http://localhost:3001
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 NEXT STEPS

  1. Verify all pods are running:
     kubectl get pods -A | grep platform

  2. Check the lifecycle controller:
     kubectl logs -n platform-control-plane -l app=lifecycle-controller

  3. Enable shadow traffic (if not already):
     kubectl annotate ingress vcai-ingress \
       -n {{ .Values.versions.v2.namespace }} \
       nginx.ingress.kubernetes.io/mirror-uri="/api/v1"

  4. Monitor the gray-scale rollout:
     kubectl argo rollouts get rollout vcai-rollout \
       -n {{ .Values.versions.v2.namespace }} -w

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION

  Quick Start:     docs/three-version-quickstart.md
  Architecture:    docs/architecture.md
  Operations:      docs/operations.md
  Troubleshooting: docs/deployment/private-offline-deployment.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Compliance Mode: {{ .Values.compliance.mode | upper }}

{{- if eq .Values.compliance.mode "hipaa" }}
⚠️  HIPAA mode is enabled. All data is encrypted and audit logging is enforced.
{{- end }}

{{- if eq .Values.compliance.mode "fedramp" }}
⚠️  FedRAMP mode is enabled. Network isolation is strictly enforced.
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Thank you for using CodeRev Platform! 🎉
